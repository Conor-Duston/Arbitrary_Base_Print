
#include <stdarg.h>

char *output_nums_in_base(int base, int total_numbers, ...) {
    va_list nums_to_convert;

    va_start(nums_to_convert, total_numbers);

    for (int num = 0; total_numbers; num++) {
        
    }


}

